# InkMind

- **后端**：`backend/` — Express + TypeScript + MySQL，见 `backend/README.md` 与 `backend/schema.sql`。
- **小程序**：`frontend/` — Taro 3 + React，见 `frontend/README.md`。
- **产品 / 库表说明**：`InkMind_PRD_AI_MVP.md`、`docs/`。
